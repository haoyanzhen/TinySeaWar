# Tiny Sea War Agent Guide

本文件是 Codex 进入项目后的总路引。做任何设计、程序、美术资产或数据改动前，先用它判断应该阅读哪些设计文档，以及本次改动应该落在哪一层。

## 项目总纲

Tiny Sea War 是基于 Godot 4.x 的 2D 二次元舰娘海战原型。当前目标不是完整养成游戏，而是做出可重复游玩的战斗切片，用于验证移动、侦查、隐蔽、炮击、鱼雷、主动技能、UI 可读性、美术资产接入和初始数值节奏。

MVP 的核心约束：

- 以开阔海域半即时战斗为主，不做抽卡、养成、剧情、基地和长线经济系统。
- 舰娘是情感与识别核心，航向、武器方向、射程、命中和状态通过舰装、特效、海面叠层与 UI 表达。
- 自动战斗与玩家指令并存：高价值攻击由玩家决定时机，其余武器维持自动交战。
- 战斗规则、公式、数据结构和表现层分离，Domain 层规则应能脱离 Godot 场景树测试。
- 所有数值都是首轮原型基线，允许通过实机测试持续调整。

## 文档路引

设计文档位于 `docs/`，使用数字前缀保持阅读顺序。新增文档请遵守 `docs/README.md` 的命名规则。

### 当前状态

- `docs/00_project_status.md`：当前完成度、已知缺口、验证结果和下一阶段优先级的唯一状态真源。判断某项能力是否已完成时先读；不要仅凭存在代码、配置或资产就标记完成。

### 玩法与数值

- `docs/10_game_core_mechanics.md`：核心战斗规则的玩法真源，只定义战斗中存在什么规则。改舰种职责、侦查隐蔽、攻击类别、旗舰胜负、潜航或技能规则时先读；不要在此复制按键、公式、角色值或 AI 权重。
- `docs/11_game_operation_design.md`：玩家战术意图的操作真源，负责输入、选择、航点、准心、辅助开关、设施操作、镜头和反馈。改快捷键、瞄准、交互状态或拒绝反馈时先读；效果与结算仍回到对应规则文档。
- `docs/12_combat_formula_design.md`：战斗计算顺序的公式真源，负责侦查、命中、修正叠加、装甲、伤害、投射物与胜负结算。改公式或结算顺序时必须对齐；当前所有概率攻击命中下限统一为 `5%`。
- `docs/13_balance_baseline.md`：公共平衡预算真源，负责运行倍率、舰种范围、Cost、参数上限和节奏目标。改公共范围或调参预算时参考；角色个体值不在此维护。
- `docs/14_character_balance_design.md`：角色级战斗设计真源，负责每名角色的定位、Cost、属性、舰装、技能和主要武器映射。改角色配置或个体偏离时参考；当前可运行精确值仍以 `data/` 为准。
- `docs/15_battle_level_design.md`：关卡意图真源，负责模式、地图配方、固定舰队、目标、取消条件、增援、奖励和目标胜率。改教学、挑战、自定义或进度设计时先读；实现状态只见 `00`。
- `docs/16_enemy_ai_behavior_design.md`：AI 决策真源，负责合法观察、决策层级、目标/任务/模式/技能评分、迟滞和难度质量。改 AI 意图、自动控制或难度时先读；导航算法与预算只见 `t00/t01`。
- `docs/17_play_design.md`：玩法体验的上位判断标准，负责取舍、理解、反制、节奏、规模和趣味验收。判断新玩法是否值得加入时先读；不得把体验描述实现成第二套数值规则。
- `docs/18_facility_weather_effect_design.md`：设施、局部环境、天气和时段的玩法效果真源，负责战术作用、组合数值和语义反馈。改环境或设施效果时先读；硬地形、环境运行时、设施 Domain 和表现/资产边界分别归 `35/37/38/43/44/45`。
- `docs/19_battle_simulator_design.md`：无图形模拟平台的技术契约，负责实验展开、隔离执行、确定性、事实记录、失败分类、恢复与产物。改模拟平台时先读；样本、阈值和结论边界只见 `36`。

### 数据契约

- `docs/20_data_schema_design.md`：数据契约总索引与公共约定，负责 Definition/State/Settings/Manifest/Experiment 边界、公共命名、引用和变更流程。新增数据类别或不确定字段归属时先读。
- `docs/21_combat_data_schema.md`：舰船、武器（含航空武器）、公式、投射物、技能、Buff 与公共战斗设置。改角色战斗配置或结算输入字段时先读。
- `docs/22_scene_environment_data_schema.md`：地图、地形、导航引用、天气、环境区、设施、支援任务与水雷。改场景战术数据字段时先读。
- `docs/23_level_progress_data_schema.md`：当前关卡、舰队成员、目标、接替增援与进度存档。改可玩内容或长期进度字段时先读；剩余关卡阶段只见 `t02`。
- `docs/24_ai_data_schema.md`：当前 AI 难度 Profile、技能 AI 标签与运行时记忆边界。改 AI 配置字段时先读；舰队方案和模式尚未成为配置，难度不得覆盖共享导航预算。
- `docs/25_presentation_data_schema.md`：窗口、镜头、海面 palette、投射物/武器表现和 VFX 播放参数。改纯表现配置时先读。
- `docs/26_simulation_data_schema.md`：无图形实验、场景、策略、种子、侧别、胜率评估与输出字段。改模拟实验清单时先读。

### 技术与实现

- `docs/30_technical_architecture.md`：Godot 技术基线、四层依赖、运行时拓扑、固定 Tick 和跨层原则。做架构性修改时先读；不在此维护玩法、字段、代码目录或完成状态。
- `docs/32_domain_design_phase1.md`：核心战斗 Domain 的状态所有权、命令、事件、服务协作和权威 Tick 顺序。改战斗规则代码、模拟或核心测试时先读。
- `docs/33_domain_design_phase2.md`：战斗表现架构，负责快照/事件到单位视图、动画、投射物、VFX、HUD 和资产降级的边界。改战斗表现接入时先读。
- `docs/34_implementation_map.md`：当前代码、数据、场景、工具与测试位置速查。开始实现时用它定位；规则和完成度不在此判断。
- `docs/35_scene_combat_domain_design.md`：硬地形与场景空间 Domain，负责障碍、区域、锚点、通行、舰船/鱼雷/炮弹几何和视线查询。改岛屿、岸线或空间合法性时先读。
- `docs/36_balance_testing_design.md`：平衡实验问题、比较口径、效应量、统计门禁和结论生命周期。改平衡实验、样本或报告判定时先读。
- `docs/37_environment_runtime_domain_design.md`：天气、时段、海况、水流、潮汐和局部环境区的运行时上下文。改环境状态或组合顺序时先读；效果数值仍见 `18`。
- `docs/38_facility_combat_domain_design.md`：设施、水雷、归属、任务、命令、事件和观察权限的 Domain 契约。改设施运行时或 AI/玩家设施操作时先读；玩法数值仍见 `18`。

### 技术解决方案

- `docs/technical/README.md`：跨系统技术解决方案的目录、命名和完成口径。新增技术方案使用 `tNN_topic_solution.md` 命名，不替代 `30-39` 架构与 Domain 主设计。
- `docs/technical/t00_coastal_ai_performance_solution.md`：有岸地图 AI、稀疏拓扑走廊、统一路线请求和地形查询的性能治理方案。优化港湾/岛屿 AI 调度、寻路、空间索引或性能预算时先读；当前战略请求明确不合并、不缓存。
- `docs/technical/t01_inertial_navigation_and_emergency_avoidance.md`：战略走廊、`1s` 常规动力学航迹、`0.1s` 高威胁紧急避险、等待/停车/倒车与状态切换的正式方案。改舰船航行、避岸、鱼雷规避或导航状态机时先读。
- `docs/technical/t02_level_objective_reinforcement_progress_solution.md`：23 个教学/挑战关的声明式目标、接替增援、结算与进度存档技术方案。改关卡目标运行时、增援编排或闯关进度时先读；当前 T-01 至 T-08、S-01 至 S-05 的目标/菜单/进度子集已接入，S-04/S-05 接替增援已实施，其余 10 个挑战关待实施。

### 美术与资产

- `docs/40_art_direction_design.md`：角色美术方向与通用资产契约，负责角色本体、舰装、阵营/性格/等级视觉语言和必需资产包。改角色视觉基线或通用交付项时先读；不在此维护角色数值、目录和完成状态。
- `docs/41_character_art_design.md`：角色原型、阵营/舰种气质、个体视觉方向和专属资产重点。生成或调整角色美术时必须对齐；表中战斗定位只用于美术转译。
- `docs/42_combat_art_design.md`：公共武器、投射物、发射过程、命中 VFX 和复用层级。做炮弹、鱼雷、航空、防空或反潜表现时参考；规则字段、跳字和当前表现值分别归 `21/44/data/visuals`。
- `docs/43_scene_art_design.md`：海面、天气、陆地/岛屿、场景层级和环境可读性的视觉真源。改海域或近岸环境观感时先读；不负责镜头输入、天气规则、物理目录或生产流程。
- `docs/44_ui_art_design.md`：UI 视觉语言、布局、安全区、HUD、战术叠层、跳字、弹窗、图标和资产规格。改 UI 视觉或 HUD 承载时先读；按键、关卡开放、胜负和当前接入仍见 `11/15/10/00`。
- `docs/45_art_asset_interface_design.md`：程序和美术资产之间的稳定语义接口。改运行时资产查找、manifest、绑定点或 AssetCatalog 时必须对齐；具体表现值不在此维护。
- `docs/46_character_art_asset_pipeline.md`：角色源图、透明拆件、配置生成、批处理和 QA 验收流程。改角色 processed 资产或对应 `tools/art_pipeline/` 时先读。
- `docs/47_scene_art_asset_pipeline.md`：海面、天气、局部环境、陆地、浅水、航道和设施美术的生成、后处理、manifest 与 QA。改场景环境资产生产时先读；规则几何和当前覆盖只见 `22/35/00`。

### 审计与历史

- `docs/history/31_program_design_phase1.md`：第一阶段 3v3 原型实施范围的历史存档。仅在追溯早期取舍时参考，不得作为当前架构、状态或代码位置真源。
- `docs/history/90_design_audit_round4.md`：2026-06-14 第四轮设计复核的历史存档，只用于追溯当时判断；仍未闭环的问题已迁移到独立工单。
- `workorder/20260717-design-audit-round4-unclosed-items.md`：第四轮审计仍未闭环问题的执行清单。处理碰撞伤害、两用炮共享装填、手动结束/超时、角色朝向、技能 cut-in 或轻巡鱼雷基线时先检查其状态与关闭条件。
- `docs/91_character_phase2_historical_validation.md`：第二期角色的独立历史考据，只解释原型、主要武装和舰种映射争议；当前配置与完成状态仍见 `data/` 和 `00`。
- `docs/92_character_phase2_static_balance_review.md`：第二期角色的静态纸面数值审查，只记录 Cost、范围、职责和组合风险；动态实验方法、样本与门禁只见 `36`。

## 当前项目完成状态

详细状态以 `docs/00_project_status.md` 为准。当前摘要：

### 当前战斗规则摘要

- 战斗为固定 Tick 半即时制，玩家与 AI 共用移动、射界、装填、侦查、伤害、碰撞和地形规则；击沉敌旗舰为主要胜利条件，同 Tick 双方旗舰沉没为平局，时间耗尽进入超时结算。
- 舰艇使用随航向旋转的椭圆舰装碰撞。
- 舰艇视野由侦查、隐蔽、视线和开火暴露决定，并保留接触残影。鱼雷只有进入自身最小发现距离才被观测，任一舰艇发现后同步全队；技能只改变施放者自身发现距离。
- 舰炮逐发独立抽取椭圆高斯落点：垂直发射线sigma参考战列舰主炮最远端的散射角与战列舰尺寸相当，平行方向为其一半。
- 鱼雷按物理管组逐座装填与发射，同组间隔至少 `1s`；相邻理想雷道极限距离间隔 `80`，每雷独立附加 `相邻张角 × 20% × 环境倍率` 的高斯角误差，恶劣海况与强风最高放大至 `3x`。
- 伤害按攻击合法性、落点/投射物、几何接触、命中/闪避、装甲和修正依次结算；火炮、航空、反潜和技能等概率攻击的命中下限统一为 `5%`，实体碰撞鱼雷不适用该概率下限；当前局内伤害量纲为设计值的 `0.5`，有效伤害、过量伤害和武器分类独立统计。
- 2026-08-02 将局内伤害倍率由 `0.25` 调整为 `0.5` 后，核心规则、五模式战斗烟测、伤害统计和模拟器专项均通过；此前在 `0.25` 基线下取得的胜率、时长、关卡门禁与动态平衡结论需在新基线下复验，不作为当前平衡验收结论。
- 天气与时段影响能见度、海况、武器、航空和鱼雷稳定性；港湾额外启用设施、潮汐和水雷。敌方运行量化战术 AI，玩家辅助保持受限，两者不绕过公共规则。
- 玩家与 AI 的移动使用统一航迹链，首要目标是让角色顺利、持续且高效地完成移动意图：低频战略层通过统一 Broker 请求门间距 `180`、起终接入候选 `4` 的稀疏拓扑走廊；不可直接接入的语义目标解析为动态可达且有正向进展的阶段目标；常规层约每 `1.0s` 选择推力/转向控制；只有已发现鱼雷或白名单高伤害攻击进入反应窗时才按 `0.1s` 启用紧急避险。战略请求每 Tick最多完成 `1` 条，不合并、不缓存，相同排队意图去重，分类失败后确定性退避，最终移动仍由 Domain 每 Tick权威校验。

### 完成状态摘要

- **战斗设计已完成**：战斗方式、战场设置、数值设定、伤害计算等均已完成。
- **可运行战斗切片已完成**：主菜单、四个开阔海域模式、十个海岸 3v3 验证模式、第一期 24 名角色关卡覆盖、核心战斗、玩家操作、HUD、结算、海面天气和自动测试均已有实现。
- **AI决策部分完成**：量化目标选择、舰种模式迟滞、进攻/防守/拉扯、主要武器窗口、技能收益、阵营观察隔离、弹性编组/阵列、协同预留、难度 Profile、高威胁紧急避险，以及玩家 `Z/X/C/V` 受限辅助控制已接入运行时；旧折线跟随和 `ShoreEscape / BoundaryEscape / PathRecovery` 临时航点链已退出。2026-08-02 的港湾/长列岛/环形泻湖 11v11、每场 `400 Tick` 定向复验均为路线失败 `0`、结束等待 `0`，长列岛/环形泻湖碰撞由 `91/302` 降至 `36/44`；重复路线失败已修复。但三图仍有碰撞、港湾绝对 P95/P99 仍超标，且尚未重做带画面交通复核。完整动态平衡、瓶颈交通、人工键鼠航行、港湾潮汐查询、可抢占单条路线预算和开阔 11v11 决策峰值仍待整改与复验。
- **战斗模拟器部分完成**：无图形实验运行、固定种子、侧别交换、双方最新运行时 AI、逐舰分类伤害、聚合统计和多格式报告已实现；参数覆盖、候选配对 A/B、并行恢复和完整动态指标仍待继续。
- **数据契约首轮拆分已完成**：`20` 只保留索引与公共约定，战斗、场景、关卡、AI、表现和模拟字段分别由 `21-26` 维护；新增字段应直接更新所属专题契约。
- **十套海岸地图已接入运行时**：十类岛屿均有独立的 `6144 × 3456` 16:9 大型海岸变体、`3840 × 2160` 美术母版，并按最终人工确认以 `2.6 × 3.2` 非等比显示；视觉、硬地形、浅水/航道、设施锚点、审核出生、三档共享导航和小地图均从同一作者变换重建。环形泻湖保持六入口版本。完整生产流水线、十图三档空间专项、十图固定种子 3v3、三分辨率正式镜头矩阵与十图 F9 对齐均通过；人工键鼠近岸航行和有岸大编队交通/绝对性能未通过关闭条件。自定义战斗已使用新变体，既有正式教学/挑战关暂留兼容地图待单独迁移。港湾额外包含潮汐、观察站、岸炮、补给维修、机场任务、水雷、设施生命周期和场景兼容 AI。其余九图尚待正式教学/挑战任务、局部环境、设施与平衡验收。
- **角色设计第一期完成**：第一期 24 名角色的数据、武器、技能和表现映射已加载并进入现有关卡。
- **角色设计第二期部分完成**：第二期 24 名角色的数据、武器、技能和表现映射已加载，但未进入现有关卡。
- **角色美术部分完成**：第二期仅 4 名完成 processed 资产，其余 20 名仍待生产。
- **战斗美术部分完成**： 战斗美术基本完成，航空、防空、反潜等效果仍需继续。
- **环境美术部分完成**： 海面、十张 16:9 大型海岸母版与运行时贴图、港湾浅水/航道、局部天气和设施已形成运行闭环；十图正式战斗镜头与 F9 对齐已完成，人工键鼠近岸航行仍待验收，其余岛屿关卡的局部环境与设施仍需继续。
- **UI美术部分完成**： UI美术资产与接口已建立，全面换肤仍需继续。
- **玩法策划部分完成**：已有核心规则、关卡和 AI 设计基础，23 个教学/挑战关的目标、接替增援和进度存档契约已经定稿；T-01 至 T-08、S-01 至 S-05 已接入正式数据、目标、菜单和进度子集，S-04/S-05 接替增援已接入运行时；其中 T-01/T-02/T-03/S-01 的正式 20 场门禁通过，T-05 至 T-08 的三轮设计路线门禁均通过，T-04 未通过，S-02 至 S-05 待完成门禁与人工验收。其余 10 个挑战关、奖励表现、完整舰种生态和动态平衡仍未形成运行闭环。
- **音效与音乐未完成**：当前没有正式音频设计、运行时音频资产、总线或播放代码，任何音频类别都不得标记为已完成。

## 代码、数据与资产路引

- `autoload/data_registry.gd`：运行时数据与资产总入口。
- `data/ships|weapons|skills/`：第一期与第二期角色规则配置；`data/levels/` 目前只有第一期角色关卡。
- `data/visuals/`：投射物表现、武器表现和 VFX 播放参数。
- `scripts/application/battle_session.gd`：战斗流程和当前大部分规则协调。
- `scripts/application/navigation/route_planner.gd`、`navigation_request_broker.gd`、`trajectory_planner.gd`：稀疏战略走廊、统一请求预算和常规/紧急动力学候选；`scripts/domain/services/ship_motion_service.gd` 负责同源推进、转向、水流、制动与简化倒车积分。
- `scripts/presentation/battle/ship_unit_view.gd`：角色战场视图；`animation_state_machine.gd` 管理角色动画状态。
- `scripts/presentation/battle/battle_effect_director.gd`：消费战斗事件并协调角色、投射物、炮弹轨迹、VFX 和伤害跳字。
- `scripts/presentation/battle/projectile_view.gd`、`shell_flight_view.gd`、`battle_vfx.gd`：公共战斗表现节点。
- `assets/characters/{character_id}/processed/`：已完成角色运行时资产；只有 `postprocess_plan.json` 不代表资产完成。
- `assets/vfx/`：公共战斗与角色模板特效；`assets/environment/`、`assets/environments/`：陆地、天气和海面资源。
- `tools/data/`：角色批次配置生成；`tools/art_pipeline/`：角色、UI、场景和陆地资产处理与验收。
- 当前不存在正式 `assets/audio/`；新增音频前必须先补音频设计、语义事件和总线约定。

## Codex 工作指引

### 开始任何任务

- 先检查 `git status --short`，保护用户已有改动，不回滚无关文件。
- 先读 `docs/00_project_status.md`，确认本次任务是在补缺口、扩展部分完成项，还是维护已完成基线。
- 根据任务类型阅读上面对应文档；如果涉及代码落点，再读 `docs/34_implementation_map.md`。
- 用 `rg` 或 `rg --files` 查找引用和调用点，避免只改一处留下旧路径、旧字段或旧规则。
- 只改和任务直接相关的文件。设计文档、数据、代码、美术资产不要顺手大范围重构。

### 设计改动

- 审查一个或一组编号设计文档的完整性、边界、重复、矛盾、冗余、拆分、合并、存档或删除候选时，使用项目级 `$tiny-sea-war-audit-design-docs`；实施审查结论时必须同步 `docs/00_project_status.md`、本文件、`docs/README.md` 和全部受影响路引。
- `10-19` 已按“核心规则、操作、公式、公共基线、角色、关卡、AI、体验、环境、模拟平台”划分唯一职责。跨文档引用应指向真源，不在非权威文档复制完整公式、数值表、实现状态、代码路径或测试历史。
- 改核心规则时，同时检查玩法、公式、数据结构、Domain 和操作文档是否需要同步。
- 改数值时区分三层：`13_balance_baseline` 是范围，`14_character_balance_design` 是角色草案，`data/` 是当前可运行配置。
- `20-26` 数据契约只保留字段名、类型、必填性、枚举、默认值、引用和加载校验；玩法语义、数值依据、Domain 状态机、实现完成度、文件入口和实验结论分别回到对应真源。
- 改美术职责时保持边界：角色方向/个体/生产归 `40/41/46`，公共战斗特效归 `42`，场景视觉归 `43`，UI 归 `44`，资产语义接口归 `45`，场景资产生产归 `47`。
- 审计报告只记录历史判断；如它与编号较低的主设计文档冲突，优先更新主设计文档，并在审计相关处说明状态。

### 程序改动

- Godot 运行时代码按现有目录分层：`scripts/domain` 放规则，`scripts/application` 放流程协调，`scripts/presentation` 放场景和 UI，`scripts/infrastructure` 放数据、资产和外部支持。
- Domain 层不要依赖 Godot 场景树、HUD 节点或具体贴图路径。
- 表现层不要重新实现命中、伤害、装填、侦查等规则；这些规则应来自 Domain 或配置。
- 资产路径优先通过 `DataRegistry.assets` 或 `scripts/infrastructure/assets/asset_catalog.gd` 的语义查询，不在表现代码里手拼文件名。
- 新增配置字段时，同步更新所属 `docs/21-26` 专题契约、加载校验和相关测试；只有新增类别或公共约定变化时才更新 `docs/20_data_schema_design.md`。

### 美术资产与管线

- 角色运行时资产真源在 `assets/characters/{character_id}/processed/`，原始 sheet 和 QA 产物不要混作运行时入口。
- UI 运行时语义清单参考 `assets/ui/qa/ui_asset_manifest.json`，导出倍率目录位于 `assets/ui/export/`。
- 改角色后处理或资产验收时，优先复用 `tools/art_pipeline/` 中现有脚本。
- 改场景环境资产时先按 `47` 区分视觉图层与规则几何；贴图 alpha 和 `visual_regions` 不得直接成为碰撞、浅水或通行真相。
- 不为每个角色重复制作公共炮弹、鱼雷、命中、水柱、范围圈等资产；优先使用公共类别资产加角色覆盖层。

### 验证

- 文档重命名或字段重命名后，用 `rg` 检查旧名称是否残留。
- 程序改动后，优先运行项目已有测试或最小可行的 Godot/脚本校验。
- 资产或 UI 改动后，检查对应 QA 报告、manifest 或运行时引用是否仍然能找到文件。
- 如果暂时无法运行测试，在最终回复中明确说明原因和剩余风险。
- 更新阶段完成度时，必须同时记录数据加载、运行时接入、关卡覆盖、资产覆盖和自动/人工验收，不以单一维度代替整体完成度。
- 航迹链虽已通过现有 14 关、双方完整 AI、共 `5600` Tick 的 headless 回归，但港湾绝对 P95/P99 尚未达到 `t00` 的 `4/8ms` 目标，且单条 A* 仍不可抢占；不得把相对性能门槛通过写成整体性能验收完成。
