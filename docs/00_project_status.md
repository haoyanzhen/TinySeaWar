# 当前项目状态与后续路引

> 状态日期：2026-08-05
>
> 本文只描述仓库当前可验证的完成度，不替代玩法、数值、程序或美术设计文档。功能、资产或数据发生阶段性变化后，应先更新本文，再同步 `AGENTS.md` 中的摘要。

## 1. 状态口径

- **已完成**：已有运行入口，且当前自动校验能够覆盖主要契约。
- **部分完成**：已有设计、数据、资产或代码，但尚未形成完整可玩闭环。
- **未完成**：缺少正式方案或运行时实现，不能作为当前能力对外描述。
- “数据已加载”“资产已生成”和“关卡中可玩”是三种不同状态，不得混写。

## 2. 当前可运行基线

### 战斗规则摘要

- **战斗流程与胜负**：战斗采用固定 Tick 的半即时制；玩家与 AI 共用移动、射界、装填、侦查、伤害和地形合法性。击沉敌方旗舰即取得主要胜利条件；同 Tick 双方旗舰同时沉没按平局处理，达到时间上限进入超时结算。
- **玩家控制**：玩家可选舰、移动、集火、布置连续航点并控制主要武器、HE/AP 和主动技能。玩家舰默认不自行移动，副武器自动开火，主要武器与技能默认手动；`X/C/V` 可分别切换受限辅助航行、副武器和主要武器自动开火，主动技能始终由玩家释放。
- **移动、碰撞与地形**：舰艇按航速、转向和航向连续移动，舰装船体使用随航向旋转的椭圆碰撞。局部航迹按与 Domain 相同的 `0.1s` 固定 Tick 预测，并用静态保守碰撞场宽相加规则多边形窄相连续验证舰体半径；玩家直接目标优先全推力最大有效转向至对齐，再全推力直行。单位碰撞、地图边界、岛岸、浅水、航道、潮汐和雷区最终仍由 Domain 规则处理；炮弹、鱼雷与光学视线读取各自的地形阻挡掩码。
- **侦查与隐蔽**：舰艇按侦查距离、隐蔽距离、视线和开火暴露建立阵营视野，失去接触后保留有限时间残影。敌方鱼雷不是全图可见；任一舰艇进入鱼雷自身最小发现距离后自动观测，并立即同步全队，技能只修改施放者自身的鱼雷发现距离。
- **舰炮**：舰炮逐发独立抽取椭圆高斯落点：垂直发射线sigma参考战列舰主炮最远端的散射角与战列舰尺寸相当，平行方向为其一半。弹轨迹、岛岸阻挡、落点圆与舰装椭圆相交、命中/闪避和伤害结算共用同一固定落点。
- **鱼雷**：鱼雷按物理管组逐座拥有射界和装填，一次命令只发射一座，同组相邻发射至少间隔 `1s`。单座联装的相邻理想雷道在极限射程处间隔 `80`；每枚鱼雷再独立附加 `相邻张角 × 20% × 环境倍率` 的高斯角误差，海况与风速最高放大至 `3x`。鱼雷离膛后各自保存方向、位置、航程、碰撞和观测状态。
- **命中与伤害**：攻击依次经过控制权限、装填、目标/射程/射角、固定落点或实体投射物、几何接触、命中/闪避、装甲补正和伤害修正。所有采用概率命中公式的火炮、航空、反潜和技能攻击统一使用 `5%` 命中下限；实体碰撞鱼雷不适用该概率下限。当前局内基础伤害、作战能力系数和装甲减伤系数统一使用设计量纲的 `0.5`；有效伤害、过量伤害和武器分类分别记录。
- **环境、设施与 AI**：天气和时段影响能见度、海况、武器命中、航空条件与鱼雷稳定性，并与局部海雾、飑线、高海况和背风水域叠加。港湾关卡还启用潮汐、观察站、岸炮、补给维修、机场任务和水雷。敌方使用量化战术 AI；玩家侧只在显式开启辅助时使用受限规则，二者最终仍通过同一战斗合法性与结算管线。

### 已完成

- Godot 4.x 项目可从 `scenes/menu/main_menu.tscn` 进入主菜单；玩家入口已统一为教学、挑战、自定义。`T-01` 至 `T-08` 和 `S-01` 至 `S-05` 已接入正式数据、菜单、目标 HUD 与任务结算；S-04/S-05 的审核接替增援也已接入。T-01/T-02/T-03/S-01 的正式 20 场门禁通过，T-04 已可运行但门禁未通过；T-05 至 T-08 各自完成三轮种子集合不相交的 20 场设计路线实验，合计 `240/240` 有效、玩家完成率和路线/动作证据均为 `100%`，技术上限、寻路卡死、路线不可用、策略拒绝、行为异常和禁止阶段伤害均为 `0`。T-05 至 T-08 的人工操作与画面可读性仍待验收；S-02 至 S-05 的 20 场门禁与人工验收待执行，其余 10 个挑战条目保持待接入。自定义可选择规模、已验收地图、20 套天气/时段并从全角色锁定列表编成己方舰队，再进入 `scenes/battle/prototype_battle.tscn` 完成战斗、结算、重开和返回主菜单流程。
- 已配置 `1v1`、`3v3`、`5v5`、`11v11` 四个开阔海域关卡，当前关卡合计覆盖第一期 24 名角色。
- 战斗切片已具备移动、索敌与接触残影、舰炮逐发椭圆高斯落点与真实轨迹、鱼雷距离观测与阵营共享、逐座鱼雷管控制、逐雷固定种子高斯角误差与天气放大、自动交战、手动主要武器、HE/AP 切换、技能、投射物、伤害、击沉、旗舰胜负与超时结算。
- 玩家操作已具备数字键选舰、鼠标选择/移动/集火、`Z` 连续多航点、`X` 受限辅助航行、`C/V` 副/主武器自动开火开关、`Cmd/Alt + X/C/V` 舰队范围、主要武器瞄准、弹药切换、技能释放、`G` 镜头跟踪、镜头移动/缩放和暂停。玩家技能始终只由玩家释放。
- 主菜单、战斗 HUD、小地图、战斗日志、选中单位面板、暂停面板和结算界面已有可运行实现。
- 海面 Shader、20 套气候与时间组合、独立天气层和镜头边界已接入运行时。
- 20 套海面气候/时间组合已通过同一 `ocean_palette` 接入全局战斗环境：天气和时段共同影响光学能见度、基础海况、武器命中与航空条件，再与局部海雾、飑线、高海况、背风和月光区域组合。
- 已新增可从主菜单进入的 `3v3 港湾入口` 近岸验证关卡。审核地形、浅水/航道、共享导航、岛岸对舰船/鱼雷/炮弹/光学视线的阻挡、七类软地形、八类岸基设施状态、战场与小地图表现均已接入。
- 十类岛屿均已建立独立浅水/航道语义模板和非规则岸线泥沙/碎浪层；港湾地图可完成“从正式数据加载 -> 编辑语义与设施/环境 -> 回写 -> 烘焙 -> 校验 -> 导航/小地图 -> QA -> 运行”的确定性流水线。
- 港湾地形查询已使用真实空间索引；水深合法性覆盖整段路线和权威移动，固定雷区按阵营知识过滤并可在 F9 全知调试层检查。
- 港湾场景战术结果已形成运行闭环：最终海况影响机动、武器和航空，潮汐控制潮滩进入，观察站提供光学接触，雷达提供独立且阵营隔离的精确位置接触；岸炮引用经典战列舰耐久防护与单座联装 AP/HE 炮塔，并受压制和同阵营通信控制；通信站普通占领不转移下游，整套易手使用显式关卡事件；补给产生装填与技能收益，维修泊位建立 `Docked -> Servicing`、定点保持、重击/移动/失效/沉没中断和 80% HP 上限；机场侦察/巡逻/空袭按准备与离场阶段真实生效；水雷控制站支持 10 秒确定性区域布雷、逐枚地形失效、无阵营豁免、共享鱼雷发现与公共伤害结算，已布设水雷独立于控制站存续。设施生命、所有权、运行和交互状态已拆分，依赖同时检查状态与阵营，并支持累计压制、伤害下限、静默、失能、恢复和摧毁。
- 敌方港湾兼容 AI 会争夺已知设施、为低耐久单位寻找维修、使用机场支援、规避已知雷区并读取公开海况/潮汐；所有结果仍经过玩家共用的命令、路线、侦查和伤害服务。
- 玩家与 AI 的旧折线跟随已由统一航迹链替换。路线系统以“角色顺利、持续且高效地完成移动意图”为首要目标：低频战略层通过统一 Broker 请求稀疏拓扑走廊门，无法直接接入的语义目标会解析为动态可达且有正向进展的阶段目标；常规层约每 `1.0s` 从真实状态按 `0.1s × 6.0s` 同源固定 Tick 展开有限候选，先用碰撞场排除远岸精确查询，再对不确定短段做带舰体半径连续窄相；安全且保持意图的开水模板可提前接受，近岸/恢复场景继续比较所需候选。只有已发现鱼雷或白名单高伤害攻击进入反应窗时才启用 `0.1s` 紧急避险。战略请求不合并、不缓存，每 Tick最多完成 `1` 条；相同排队意图去重，分类失败后确定性退避，无安全旧走廊时停车等待，必要时允许简化倒车；Domain 拒绝已承诺位移时计划立即失效，最终位移始终由 Domain 每 Tick权威校验。
- 无图形战斗模拟器首个可用版本已接入：实验清单可配置关卡、种子、固定 Tick、保护上限、侧别交换及双方独立策略；既可使用稳定基线策略，也可让双方运行当前完整量化 AI，并输出单局 JSONL、聚合 JSON、CSV、Markdown 和逐舰分类伤害报告。

### 部分完成

- **玩法策划：部分完成。** 核心战斗规则、操作、公式、数值基线、关卡草案和敌方 AI 方案已经存在；23 个教学/挑战关的声明式目标、接替增援与进度存档契约已经确定。`T-01` 至 `T-08`、`S-01` 至 `S-05` 已形成正式数据、目标结算、菜单和进度子集，S-04/S-05 接替增援已进入运行时；T-01/T-02/T-03/S-01 的 20 场门禁通过，T-05 至 T-08 的三轮设计路线门禁均通过，T-04 为 `18/20` 有效、`94.4%` 对目标 `100%`，未通过。其余 10 个挑战关、奖励表现、完整舰种生态和人工实机平衡验收仍未完成。
- **AI 决策：部分完成。** 敌方已接入量化目标、模式/战术迟滞、主要武器与技能窗口、高威胁紧急避险、长时间消极行为的接敌压力、阵营观察隔离、设施任务、伤害/效果预留、弹性编组与阵列、岛岸掩体，以及简单/标准/困难 Profile；玩家辅助仍维持独立受限能力。战略路线已改为稀疏拓扑走廊，旧 `ShoreEscape / BoundaryEscape / PathRecovery` 临时航点链不再进入运行时。全速转弯擦岸、船体扫掠体和同 Tick 连续验证的自动实现已于 2026-08-03 完成；2026-08-05 又在不减少候选、不缩短预测窗和不放宽安全的前提下，将静态浅水/礁区限制位并入 v2 碰撞场、为空间窄相增加边索引，并按可达包络跳过不可能跨越的局部运动区重复采样。共享优化已推广至开阔与十种岸岛地图：固定种子同一 11v11 舰队下，十张岸岛各 `400 Tick` 的路线失败、岸线碰撞、航迹失败和运行等待均为 `0`，导航 mean/P95/P99 平均为 `4.598/7.590/9.124ms`，范围为双岛长水道 `3.513/5.864/7.049ms` 至港湾 `6.288/10.446/10.984ms`。绝对 `4/8ms` 门禁、完整动态平衡、带画面瓶颈交通、人工键鼠航行、港湾潮汐阶段切换、可抢占单条路线预算和开阔 11v11 决策峰值仍待整改与复验。
- 十套岛屿均已各自生成 `6144 × 3456` 的 16:9 大型海岸运行时地图、`3840 × 2160` 透明美术母版、`1920 × 1080` 运行时贴图、每侧 11 个大型深吃水安全出生槽、三档共享导航图、小地图遮罩和 3v3 等 Cost 验证入口；海岸统一与海面中心对齐，并按最终人工确认使用 `2.6 × 3.2` 非等比显示，视觉实例、硬地形、浅水/航道、设施锚点与导航均从同一作者变换重建。环形泻湖固定保留六个入口：北、南主入口净宽 `728` 世界单位、`NavigationChannel` 宽 `624`，新增西北、东北次入口净宽 `520`、通道宽 `416`，东、西保留原有开口。自定义战斗的 1v1/3v3/5v5/11v11 均可选择十图并按规模读取前 `1/3/5/11` 个槽。港湾额外保留局部环境、设施、机场和水雷完整战术闭环。生产流水线、十图三档空间专项、十图固定种子 3v3 烟测、三分辨率正式镜头矩阵和十图 F9 对齐均通过；人工键鼠近岸航行与有岸大编队交通/绝对性能未通过关闭条件。当前 16:9 变体用于自定义与近岸验证入口，既有正式教学/挑战关仍保留兼容地图定义，待单独迁移并重做路线门禁；其余九图尚未完成正式任务编排、局部环境配置和动态平衡验收。
- 航空、防空、反潜和潜航已有规则或配置基础，但表现、AI、关卡覆盖及动态平衡仍需专项验收。
- 战斗模拟器的 Definition 参数覆盖、候选参数配对 A/B、并行恢复、完整 AI 动态指标和编成抽样尚未实现；当前版本适合固定配置回归与首轮数值筛查，不代表完整平衡工具链完成。

## 3. 数据、角色与美术状态

### 已完成

- 数据契约已完成首轮拆分：`docs/20_data_schema_design.md` 只保留总索引与公共约定，具体字段分别由 `21` 战斗、`22` 场景环境设施、`23` 关卡进度、`24` AI、`25` 表现和 `26` 模拟实验维护。规则、数值依据、Domain 状态机、实现状态、代码位置和实验结论不再由 Schema 重复拥有。
- 运行时配置注册表当前加载 48 名角色定义：第一期 24 名和第二期 24 名。
- 第一期开局数据、武器、技能和四个原型关卡已接入；第二期 24 名角色的数据、武器、技能与武器表现映射已进入配置校验。
- 角色运行时资产通过 `AssetCatalog` 按动画、VFX、绑定点、战斗图层和 UI 图层查询。
- 已有 `ShipUnitView`、角色动画状态机、战斗表现导演、投射物视图、炮弹飞行轨迹、VFX 播放和伤害跳字模块。
- 公共 UI、战斗 VFX、海面/天气、陆地素材以及相应 manifest 或语义配置已经建立。

### 部分完成

- 48 名角色中，当前有 28 名具备 processed manifest；第一期 24 名完整，第二期仅 4 名完成并通过运行时资产发现，第二期其余 20 名仍只有生产计划或未完成的处理资产。
- 第二期 24 名角色虽然能被配置注册表加载，但尚未加入现有四个可玩关卡；不得描述为“48 名角色均已可玩”。
- 当前菜单会尝试读取全部角色定义的封面；缺少 processed UI 资产的 20 名第二期角色会产生资源缺失错误日志，随后被候选列表排除。这是已知待修复项。
- UI 已有大量导出资产，但部分 HUD、面板和按钮仍由程序绘制，尚未全部替换为正式 UI 资产。
- 火炮、鱼雷、VFX 和角色动画已有运行表现；舰载机编队、防空交互、反潜与更多角色专属表现仍未完整接入。

### 当前资源目录边界

- `assets/environment/land/source/` 只保留已审核的 16:9 海岸透明母版、构图参考和权威参考遮罩；`assets/environment/land/land_*_16x9_runtime.png` 及相关 manifest 是运行时入口。
- `assets/environment/land/generated/` 为 Git 忽略的临时工作区，raw/chroma-key 中间图不再作为资产交付保留；正式资源清单也不再引用它们。
- `assets/environment/qa/` 只保留文档或 manifest 引用的最终审查证据。组合图所需的单张临时面板改在系统临时目录生成，单次人工输出默认写入被忽略的 `reports/`。
- `addons/terrain_authoring/` 是仅编辑器使用的作者插件；运行时注册表源码位于 `scripts/infrastructure/bootstrap/data_registry.gd`，Autoload 名称仍为 `DataRegistry`。
- `artifacts/simulations/` 保留被忽略的可再生模拟报告供本地复核，但不是配置真源；`artifacts/.gdignore` 将整个生成物树排除出 Godot 资源扫描。

## 4. 明确未完成项

- **音效与音乐：未完成。** 当前没有正式音频策划文档、`assets/audio/` 运行时资源、音频总线配置或 `AudioStreamPlayer` 播放实现。炮击、命中、鱼雷、航空、UI、环境、BGM 和语音均不能标记为已完成。
- **整体玩法策划：未完成。** `10-19` 已完成一轮职责边界、重复内容和矛盾口径审计，核心规则、操作、公式、公共基线、角色、关卡、AI、体验、环境和模拟平台各自拥有明确真源；文档边界清楚不等于运行闭环完成，模式、编成、其余关卡、奖励、舰种生态和人工平衡验收仍有缺口。
- 自定义战斗编成与十三个正式关卡已有运行时实现；其余 10 关、完整章节流程和奖励表现尚未实现。进度存档已具备挑战成功与舰船解锁的三槽事务契约，S-01 至 S-05 首通奖励与顺序解锁已接入，但仍需随后续关卡扩展覆盖。
- 第二期角色的完整美术生产、关卡接入与全量动态平衡没有完成。
- 十套岛屿的运行时验证入口均已建立，但九套非港湾地图尚未进入正式教学/挑战关编排，也未配置局部环境与设施；当前接入结果不能替代关卡节奏、美术和动态平衡验收。
- 发布构建、平台适配、性能预算、无障碍与完整用户测试尚未形成验收结果。
- 碰撞场与同 Tick 航迹已通过十图 3v3 和三张高压 11v11 headless 自动正确性门禁，局部航迹相对 2026-08-05 原始复测已显著降本，但三张高压图仍未达到 `t00` 的绝对 `4/8ms` 目标，字段 P99 约 `2.1ms` 也略高于 `2ms` 预算；单条 A* 尚未改成可抢占的节点/微秒预算，带画面大编队交通和人工键鼠近岸航行未复验，不得描述为整体性能或人工验收完成。

## 5. 最近验证

- 2026-08-05：分两轮优化局部航迹实现开销，不改变 `0.1s × 6.0s` 固定预测、候选上限、`96` 舒适净空带、连续舰体扫掠或 Domain 权威校验。除标量运动积分、恒定环境复用和批量字段诊断外，碰撞场 Schema/算法升级至 v2，在同一次 `8 × 8` 格遍历中返回浅水/礁区限制位；岸线精确回退使用 256 单位边索引；港湾局部运动区预排序并以可达包络决定是否需要逐样本上下文。20 图字段双烘焙一致，硬地形 `19737/19737` 个肯定安全样本零误判，限制水域额外避免 `33479` 次精确检查。长列岛 11v11 的候选 `2922`、字段查询 `175320`、已接受模拟段 `148972`、地形拒绝候选 `471` 与原始复测一致，四项失败仍为 `0`；导航 mean/P95/P99 由 `14.245/37.686/45.818ms` 降至 `4.432/7.721/9.243ms`，Tick 由 `31.392/61.334/70.590ms` 降至 `16.097/22.415/30.261ms`。港湾/环形泻湖 Tick mean/P95/P99 分别为 `22.075/30.814/36.582ms`、`16.589/22.479/26.848ms`。候选审计显示第 3 候选及以后仍有实际胜出，上一轮预测后缀严格可复用仅为长列岛 `23/834`、港湾 `36/831`、环形泻湖 `30/830`，因此未直接砍候选或缓存旧控制。专项 `74/74`、航迹 `28/28`、路线恢复 `10/10`、海岸运行时 `12306/12306`、地形作者 `10/10` 通过；绝对性能、带画面交通和人工航行仍未关闭。

- 2026-08-05：以同一 11v11 舰队、种子 `20260711`、双方完整 AI 串行复验开阔与十张岸岛；岸岛各执行 `400 Tick`，路线失败、岸线碰撞、航迹失败和运行等待合计均为 `0`。十图导航 mean/P95/P99 平均 `4.598/7.590/9.124ms`，Tick 平均 `16.505/22.649/27.297ms`；双岛长水道导航最轻 `3.513/5.864/7.049ms`，港湾最重 `6.288/10.446/10.984ms`。开阔海域于第 `327 Tick` 正常结束，导航为 `4.209/8.111/10.101ms`，无静态字段成本。全表见 `t00` 14.13；正确性推广通过，但绝对 `4/8ms`、字段 `2ms` P99、带画面和人工验收仍未关闭。

- 2026-08-05：将性能模式按权威 Tick 顺序拆为互斥阶段，并对侦查与 AI 决策下钻。长列岛 11v11×400 Tick 的 Tick mean/P95/P99 为 `16.039/22.140/29.868ms`；单位侦查观测、局部航迹、移动、AI 决策分别占均值 `30.3%/27.5%/14.0%/8.2%`。战略路线均值只占 `5.5%`，但 22 条请求的单条 A* mean/P95/max 为 `13.013/17.058/17.204ms`，使战略阶段 P99 达 `19.504ms`。非导航合计占 `67.0%`，但战略+局部导航仍是最大系统家族 `33.0%`；下一稳定热点为单位侦查 `4.846ms`，下一尾延迟热点仍是不可抢占 A*。四项故障为 `0`，互斥未归类仅 `0.010ms`；完整表见 `t00` 14.14。

- 2026-08-03：实施导航静态碰撞场和同 Tick 连续航迹。20 个 TerrainMap 新增由 `ShipMovement` 规则多边形与地图边界确定性派生的 `8 × 8` 占用 BitMask/距离下界场、manifest、checksum/revision 加载门禁和精确查询回退；常规/紧急候选改为与 Domain 相同的 `0.1s` 固定 Tick 展开，玩家直接目标优先全推力最大有效转向至实际对齐后直行，近岸候选按净空和进弯速度评分，Domain 碰撞立即使计划失效；静止或向外脱离只受额外软余量影响时保留安全候选，不重复报告无解。碰撞场双烘焙与全部 20 图三半径及 `±1` 浮动值差分通过，随机点/线段、岸线切线和地图边界的 `19737/19737` 个肯定安全结果假阴性为 `0`；专项 `68/68`、航迹 `28/28`、路线恢复 `10/10`、海岸运行时 `12306/12306` 通过。十图 3v3 共 `4000 Tick` 与港湾/长列岛/环形泻湖 11v11 各 `400 Tick` 均为路线失败、岸线碰撞、航迹失败、结束等待 `0`；11v11 字段 P95/P99 最差 `1.558/1.646ms`，精确窄相比例最高 `3.56%`。核心与五模式烟测为 `1770/1771`，唯一失败是本次改动前已存在的第二期角色 processed/VFX 资源目录缺失，不属于导航回归。港湾 `4/8ms` 绝对门槛、长列岛/环形泻湖候选成本、带画面大编队交通和人工键鼠航行仍未关闭。

- 2026-08-02：完成环境资源目录清理。删除十图共 20 张可再生的 raw/chroma-key 海岸中间图，以及未被文档或 manifest 引用的旧审查图；保留已审核源母版、构图参考/遮罩、运行时贴图和正式 QA 证据。海岸资产清单与生成清单不再引用 `generated/`，场景综合 QA 的中间面板改用系统临时目录，单独地形审查默认输出至被忽略的 `reports/`。同时清理 `.DS_Store`、Python 缓存、空占位目录和模拟输出中的 Godot `.import`/`.translation` 副产物，并以 `artifacts/.gdignore` 隔离可再生模拟报告。综合场景 QA 已成功生成到系统临时目录，Godot 编辑器 headless 加载无错误、无资源扫描警告；此项只收口资源所有权与目录卫生，不改变玩法完成度。

- 2026-08-02：收口 Godot 编辑器插件与 Autoload 的目录及状态所有权。保留 `addons/terrain_authoring/` 作为仅编辑器使用的独立插件边界，并补充正式数据、工具依赖、操作和发布说明；`DataRegistry` 源码从顶层 `autoload/` 迁至 `scripts/infrastructure/bootstrap/`，Autoload 名称保持不变。自定义关卡定义改由 `GameFlow` 持有并通过 `BattleSession.create_battle_from_definition()` 创建，不再写入 `ConfigRegistry` 的正式定义集合。Godot 编辑器 headless 加载无错误，核心规则与五模式烟测 `1771/1771`、场景表现与自定义战斗 `226/226`、地形作者插件 `10/10` 通过。仓库仍无正式 `export_presets.cfg`，插件与测试的发布排除规则需在创建目标平台预设时落地并检查导出包。

- 2026-08-02：修复有岸大编队重复路线失败。根因是无接触搜索的语义目标落入中央陆地区域且目标侧无法在 `420` 范围接图，失败后同一意图每 `3s` 重提；设施 `FACILITY_CONTROL_NOT_ALLOWED` 拒绝是独立问题。运行时现保留语义目标，在动态可达分量中选择正向阶段目标，起点接图仅做一次 `840` 恢复尝试，失败原因分类，并对排队/失败意图去重退避；舰队搜索航道按稳定 ID 横向展开。固定种子 `20260711` 的港湾/长列岛/环形泻湖 `11v11 × 400 Tick` 均为路线失败 `0`、结束等待 `0`、动力学航迹失败 `0`，岸线碰撞为 `35/36/44`。核心回归新增阶段目标与分类失败断言；路线失败缺陷关闭，但绝对性能与带画面交通门禁仍未关闭。

- 2026-08-02：局内伤害倍率由 `0.25` 调整为 `0.5`，设计量纲保持不变；基础伤害、作战能力系数和装甲减伤系数同步换算，最终局内伤害为设计结算的 50%。核心规则与五模式战斗烟测 `1769/1769`、伤害统计 `27/27`、模拟器 `41/41` 通过；固定种子 1v1、3v3、5v5、11v11 和港湾 3v3 均正常结束。此前基于 `0.25` 倍率取得的胜率、时长、关卡门禁和动态平衡结论保留为历史证据，但在 `0.5` 基线下重新运行前不作为当前平衡验收结论。

- 2026-07-28：完成十张正式 `_16x9` 海岸地图 `2.6 × 3.2` 运行时接入的自动与正式画面门禁。修正 T-04/T-06/T-08 环境区引用/绕序、过期核心夹具、场景资产清单覆盖和导航烘焙/运行时连续线段判定差异；移除仅用于比例验证的 `large_coast_scale_sample` 玩家数据与派生产物。完整场景生产流水线及确定性比较通过，地形工具负例 `9/9`、核心 `1769/1769`、地形作者 `10/10`、海岸运行时 `12306/12306`、航迹 `28/28`、场景表现 `225/225` 通过；十图固定种子 3v3 均完成创建、移动、交战和结算。正式战斗场景已生成三分辨率 × 最近/默认/最远镜头矩阵、十图 F9 对齐和五图 5v5/11v11 航行总览，最远世界视野固定为 `4096 × 2304`。人工键鼠战斗窗口未能由受控本机会话取得，仍待用户实机复核；五图 5v5/11v11 的 `400 Tick` 负载已执行但未通过，港湾 5v5/11v11 分别出现 `97/261` 次路线失败，长列岛/环形泻湖 11v11 分别出现 `91/302` 次岸线碰撞，因此工单继续保持打开，不能标记有岸大编队或绝对性能验收完成。

- 2026-07-28：按人工反馈将 16:9 环形泻湖由四入口扩为六入口。保留北、南、东、西原入口，在西北、东北岛链各新增一处净宽 `640` 世界单位、`NavigationChannel` 宽 `512` 的次入口；母版、运行时图、硬地形、视觉区域、导航、小地图和 manifest 同步到 `ring_passages_v3`。内置 `gpt-image-2` 候选因改变整体岛链构图被拒绝，第二次精修遇到网络错误，最终使用已验收 v2 母版 RGB 与确定性不规则 alpha 遮罩保持构图和细节。仅增量重烘焙 `terrain.map.ring_lagoon_16x9`，逐对象确认其余九张 16:9 地形与导航无变化；两条新增大型深吃水路线均通过。全量十图母版/运行时尺寸与 alpha `20/20`、正式地形校验、海岸运行时 `56/56`、地形作者工具 `10/10`、场景表现 `225/225` 通过，并更新六入口专项审查图。

- 2026-07-28：删除场景表现测试中“兼容解锁角色固定为 6 名”的失效断言。该数量只适用于无存档默认状态，合法进度会继续解锁角色，不应由场景测试固定；场景表现专项现为 `225/225` 通过。地图与镜头口径统一为“最远镜头下玩家可见区域宽高不超过地图对应宽高的 `2/3`”，保持 `camera.max_map_visible_fraction=0.6666667` 不变。另使用正式 `_16x9` 海岸模板、海面 Shader、各图前三个审核玩家出生槽和大和/阿芙乐尔/岛风 `ShipUnitView`，生成十张 `3840 × 2160` 纯图形模拟运行时角色比例示意图及总览并完成人工查看；该证据不替代真实战斗航行、规则几何或动态平衡验收。

- 2026-07-19：重建十张 16:9 大型海岸环境美术。每张图保留独立生成原图、透明化中间图、`3840 × 2160` RGBA 母版和 `1920 × 1080` 运行时贴图，使用审核构图遮罩固定岸线轮廓；十图在 `6144 × 3456` 海面中心以 `3.2 × 3.2` 等比显示，世界范围与旧构图 `4.86 × 3.0375` 缩放一致。新增十套 `_16x9` 模板、地图、导航、全规格安全出生、小地图、资源语义与自定义/3v3 验证入口，港湾设施、局部环境和水雷同步生成兼容变体；正式教学/挑战关暂不迁移。母版/运行时尺寸与 alpha 遮罩 `20/20`、正式地形校验、海岸运行时 `51/51`、地形作者工具 `10/10` 通过；场景表现为 `225/226`，唯一失败是本批次前已存在的“兼容解锁角色应为 6 名”断言与当前进度数据不一致。十图接触表已人工查看；因当前 Codex 图形会话提升权限额度限制，真实带画面战斗审查仍待人工执行。

- 2026-07-19：按人工反馈修正 T-07“侦查共享”。删除进入海峡南口的第二航点，只保留南侧前出侦查区；沃德停在该区即可通过公开光学规则建立兴登堡共享接触。共享接触后开放衣阿华手动主炮，但己方三舰全程禁止自动航行与自动主武器，仅副武器自动保持开启；确定性策略改为等待公开接触、射程、射界和装填窗口后提交衣阿华手动主炮命令，不再依赖自动主炮。三轮不重叠种子的正式 20 场实验均为 `20/20` 有效、玩家完成率/路线动作证据/胜率 `100%`，P10 时长分别为 `32.39s / 32.40s / 32.39s`，技术上限、路线不可用、寻路卡死、策略拒绝、行为异常与交战前敌方伤害均为 `0`；目标运行时 `76/76`、模拟器 `41/41` 与 headless 启动通过。人工画面与操作手感仍待验收。

- 2026-07-17：完成 `90–92` 审计与历史文档收口。原 `90_design_audit_round4.md` 已移入 `docs/history/`，仍可由当前仓库证据确认的碰撞伤害、两用炮共享装填、手动结束/超时、角色朝向、技能 cut-in 和轻巡可选鱼雷基线问题迁入 `workorder/20260717-design-audit-round4-unclosed-items.md`；已由后续设计闭环的旧条目不再重复追踪。`91` 补齐当前功能边界、来源用途和 Z23 抽象说明；原 `92` 改名为 `92_character_phase2_static_balance_review.md`，修正开火破隐、弹药切换人数、轻巡数量与 Z23 炮座事实，并把动态样本、策略和门禁归还 `36`。同步修复第二期伊-19显示名截断、`14` 中 Z23 联装数错误，以及四名有双弹种武器但当前未开放 `Q` 的角色说明；更新 `AGENTS.md`、`docs/README.md` 和历史引用。全量 JSON 解析、第二期配置专项 `285/285`、三份审计文档边界/引用盘点和差异格式检查通过。
- 2026-07-18：修复 T-05 至 T-08 教学关的配置锁与设计路线实现。原目标配置把移动命令错误放入交战前锁，导致玩家阵营无法执行教学路线；原 T-08 模拟报告为 `20/20 TechnicalLimit`、零命令/零侦查/零射击，不能作为验收证据。现已让 Player 与 SimulationPolicy 共用正常移动权限与航迹链，按关卡记录有序物理航点、指定来源/目标/武器类别的动作证据，并修正稀疏走廊门间距、停滞恢复和卡死报告。T-07 额外把敌方武器锁精确延续到衣阿华首次共享目标主炮命中，移动、侦查、受击和公共伤害规则不变。T-05 至 T-08 各运行三轮互不重叠种子的正式 20 场实验，合计 `240/240` 有效、胜率/完成率/路线动作证据 `100%`，技术上限、路线不可用、寻路卡死、策略命令拒绝、行为异常和禁止阶段敌方伤害均为 `0`；专项测试为目标 `68/68`、模拟器 `41/41`、航迹 `28/28`、路线恢复 `10/10`。本结果不替代人工 HUD、操作手感和画面可读性验收。

- 2026-07-17：完成 `docs/40_art_direction_design.md` 至 `docs/46_character_art_asset_pipeline.md` 的逐文件与交叉边界审计。`40/41` 收口为角色视觉基线与个体美术输入，`42` 只保留公共战斗表现，`44` 只保留 UI 视觉与承载，`45` 只保留稳定资产接口，`46` 只保留角色资产生产；修正第二期角色资产状态、天气表现越权、菜单层级、`G` 跟踪、`X/C/V` 辅助开关、主动技能手动释放、同 Tick 双旗舰沉没平局、表现配置归属和失效工具名等过期或矛盾描述。原 `43` 中的场景视觉目标继续保留，生成、后处理、目录、manifest 与 QA 拆入新增 `docs/47_scene_art_asset_pipeline.md`；本轮没有满足删除或历史存档条件的 `40–46` 文件。`AGENTS.md`、`docs/README.md`、`34`、`33` 与项目级审计 skill 路引已同步；8 份文档盘点无缺失引用或无消费者文件，skill 结构校验、Shell 语法和 Markdown 差异格式检查通过。本轮只修改文档与审计流程，没有运行 Godot 测试。

- 2026-07-17：新增项目级 `$tiny-sea-war-audit-design-docs`，固化编号设计文档的逐文件与跨系列审查顺序、权威来源优先级、完整性/边界/重复/矛盾/冗余检查、Keep/Tighten/Split/Merge/Archive/Delete candidate 判定、删除授权门槛、存档移动处理，以及 `00`、`AGENTS.md`、`docs/README.md` 和其他路引的同步要求。skill 同时提供只读盘点脚本，用于报告规模、开头边界、重复标题、旧状态候选、缺失文档引用、历史引用和入站消费者；`quick_validate.py`、Shell 语法检查、30/35 与 37/38 定向试运行、全量编号文档盘点，以及无边界/无引用/无消费者的空文档边界用例均通过。

- 2026-07-17：完成 `docs/30` 至 `docs/36` 的技术文档边界审计与收口。`31_program_design_phase1.md` 移入 `docs/history/`；`30` 只保留架构，`32` 只保留核心战斗 Domain，`33` 只保留表现架构，`34` 只保留当前实现位置，原 `35` 拆为硬地形 `35`、环境运行时 `37` 和设施 `38`，`36` 补齐无效运行、配对效应量/区间和结论生命周期。同步修复运行时同 Tick 双旗舰沉没仍判玩家胜利的旧实现，现统一返回空 `winner_faction` 与 `FLAGSHIP_SUNK_SIMULTANEOUS`。核心无 smoke 回归 `1746/1746`、关卡目标专项 `39/39`、AI 难度专项 `12/12`、全量 JSON 解析、Markdown 链接/旧路引/旧字段残留与差异格式检查通过；完整核心回归为 `1760/1761`，唯一失败是既有港湾 3v3 固定 smoke 在 420 秒时限内未产生要求的完整 `WeaponFired -> AttackResolved -> BattleFinished` 事件链，需作为独立场景回归继续处理。

- 2026-07-17：T-02、T-03 已完成正式 20 场门禁，均为 `20/20` 有效、玩家胜率 `100%` 且必做动作证据通过。T-04 已接入并生成正式报告，但仅 `18/20` 有效（2 局 `TechnicalLimit`），有效局胜率 `94.4%`，低于 `100%` 目标，因此明确保持未通过；不得把“有运行入口”写成“验收完成”。

- 2026-07-17：完成数据契约拆分。原 `docs/20_data_schema_design.md` 从战斗、场景、关卡、AI、表现和模拟实验混合总表缩减为索引，新增 `docs/21_combat_data_schema.md` 至 `docs/26_simulation_data_schema.md` 六份专题契约，并同步 `AGENTS.md`、`docs/README.md`、10–19 主设计和技术文档路引。AI 难度配置中无运行时消费者、且与共享导航预算冲突的 `route_candidate_count` 已从数据、加载校验和专项测试退出；该清理不改变当前导航候选数，统一预算仍由 t00/t01 技术方案和导航实现拥有。JSON 解析、Markdown 差异检查和 AI 难度专项 `12/12` 通过。

- 2026-07-16：完成 `docs/10_game_core_mechanics.md` 至 `docs/19_battle_simulator_design.md` 的逐文档边界审计。十份文档均在开头明确功能与非职责范围；概率命中公式下限统一为 `5%`，同 Tick 双方旗舰沉没统一为平局；固定舰种目标优先级、AI 难度私有导航候选预算、雷达“未来实现”、固定关卡数量、历史测试数字、代码路径副本和重复公式/天气/操作说明已从非权威文档移除。文档链接、旧口径残留和 Markdown 差异检查通过；本项不包含运行时代码或数值数据变更。

- 2026-07-16：`T-01`、`S-01` 完成正式 20 场多种子胜率实验与报告门禁。加载器强制 `LevelWinRateEvaluation` 恰好使用 20 个互不重复种子，并只按聚合战斗统计报告结算；每批同时输出 Markdown/CSV/JSON 战斗统计和逐舰分类伤害。T-01 采用确定性合法教学操作与关卡开启的受限辅助，`20/20` 完成、胜率 `100%`、平均 `86.42s`、AI 命令拒绝与行为异常均为 `0`。S-01 双方简单档最新运行时 AI，`20/20` 完成，玩家 `11` 胜 `9` 负、胜率 `55%`，进入 `60%±5%` 下界，平均 `84.20s`、AI 命令拒绝 `0`；但百眼巨人 20 场伤害为 `0`，且厌战出生横坐标在 `1090–1100` 的小范围内引发明显胜率跃迁，故人工可玩性、航母参与度和出生敏感性仍未验收。关卡目标专项 `15/15`、模拟器专项 `36/36`、Godot 启动解析通过；核心套件已删除补给完成、维修完成、不可摧毁岸炮摧毁和固定水雷伤害四项过时断言，对应正式设施契约继续由专项测试覆盖，包含五个 smoke 场景的完整核心回归 `1756/1756` 通过。

- 2026-07-15：补全十张近岸地图的全规格自定义出生组合。每张 `TerrainMap` 固定保存双方 `1..11` 槽，统一按大型深吃水半径 `46`、边界、岛岸、非法水域、同阵营分离和三档共享导航校验；自定义 1v1/3v3/5v5/11v11 分别读取前 `1/3/5/11` 槽，菜单四种规模均开放十图。40 个近岸规模×地图组合的双方映射与实际舰体出生专项并入场景测试，当前 `222/222` 通过；十图固定 3v3 兼容专项 `51/51`、地形正式校验和 9 项工具负例通过。核心套件仍为既有 4 项港湾设施旧断言失败，不外推为大型近岸战动态验收。

- 2026-07-13：正式关卡进度保存改为正式/候选/恢复三槽。新候选带递增 revision 与 SHA-256 校验，写后重新读取验证，旧正式槽在提升候选前保留为恢复槽；启动时选择三个槽中校验有效且 revision 最高者。专项覆盖连续写入、正式槽损坏、恢复槽回退、残留候选恢复和篡改校验拒绝。

- 2026-07-12：完成航迹与近岸 AI 技术基线定稿。战略层使用门间距 `180`、起终接入候选 `4` 的稀疏拓扑走廊；统一 Broker 对所有规模每 Tick最多完成 `1` 条战略请求，玩家优先，同级按提交序号，不合并、不缓存。常规航迹最多 `6` 个候选、周期 `1.0s`；紧急航迹基础 `5` 个、扩展后最多 `7` 个候选，只对白名单高威胁按 `0.1s` 运行。严格可抢占微秒预算、带画面人工航行和全部绝对性能门槛仍未完成。
- 2026-07-12：最终方案接入全部 `14` 个关卡的双方完整 AI 性能回归，共 `5600` Tick；战略路线失败、动力学航迹失败和结束等待积压均为 `0`，战略/常规/紧急单 Tick峰值分别为 `1/5/3`，紧急航迹共触发 `142` 次且未扩散。港湾平均/P95/P99 从旧方案的 `12.88/17.03/176.63ms` 降至 `6.53/10.22/12.66ms`，相对开阔 3v3 的平均/P99比率为 `1.86x/1.51x`；九张非港湾近岸图单 A*最大 `1.42-4.10ms`，港湾动态潮汐查询最大 `13.87ms`。当前全量最差项转为开阔 11v11 的既有 AI 决策，整 Tick P95/P99 为 `42.56/55.78ms`。专项航迹 `14/14`、近岸走廊 `10/10` 通过；核心无烟雾回归 `1736` 项仍只有既有 `4` 项港湾设施断言失败。

- 2026-07-10：十类岛屿均完成运行时接入。新增九套非港湾 `terrain.map.*`、审核出生、三档共享导航、3v3 等 Cost 关卡入口和小地图遮罩；主菜单使用近岸地图选择器进入全部十图。地形生产门禁与 9 项非法数据负例、十图加载/导航/出生专项 `51` 项和场景表现 `131` 项通过；导航烘焙去除正反重复边查询后输出哈希保持不变。九张新增地图各以双方最新完整 AI 完成单局冒烟，均产生 `WeaponFired -> AttackResolved -> BattleFinished` 完整事件链；其中破碎环礁、中央沙洲、新月岛、双岛长海峡、大岛偏置和环岛泻湖以旗舰沉没结束，双航道礁线、细长群岛和散岛群以时间上限结束。核心测试执行到 `1734` 项时有 `4` 项港湾设施旧断言失败，涉及补给、维修、设施摧毁和水雷伤害；该结果不外推为地图平衡或人工美术验收。

- 2026-07-10：统一测试验收口径。单个功能或性能改动默认只执行 `1 -> 3` 个固定种子；20 局及以上统计、侧别交换、平衡矩阵和规模性能验证只能由人工在任务、工单或用户指令中明确指定为大版本改动后执行。本规则为文档流程约束，不新增模拟器配置或运行时拦截；既有 20/40 局及更大批次仅作为历史证据保留，不追溯改写。

- 2026-07-10：人工指定首个平衡性子项“伤害倍率与舰炮散布”测试，使用 3v3 双方最新 AI、标准 Profile 和 20 个固定种子，输出几何相交、实际命中、有效伤害和 TTK 的 CSV、Markdown 与 PNG。20 局均正常结束：舰炮结算 `6584`，几何相交 `3067/6584`（`46.58%`），实际命中 `5034/6584`（`76.46%`），有效伤害事件 `1909/6584`（`28.99%`），平均 TTK `84.11s`。该结果只覆盖平衡性测试的一个子项，不能外推到舰队安排、Cost、地图或 AI 策略整体平衡。

- 2026-07-10：复核设计审计 `2026-06-30`、`2026-07-02`、`2026-07-03` 与 `2026-07-05`，并将原 `docs/93_ai_navigation_facility_review.md` 整理为 `workorder/20260702-ai-navigation-facility-harbor.md`。普通设施交互定点冲突、港湾等 Cost 校验及侧别交换门禁已确认修复；AI 文档验收口径/编号、近岸性能、舰炮散布后的分层数值与画面可读性、23 关运行时化和历史验证可追溯性仍未完成。定向复测中设施综合契约为 `16/16` 通过，但 AI 设施任务为 `35/36`：动态水雷任务完成后未生成预期批次，故动态布雷仍需修复回归后才能作为当前已验证能力。

- 2026-07-04：完成 `workorder/20260702-ai-navigation-facility-harbor.md` 第二章逐小节实施。玩法/操作、AI、设施、数据、Domain、关卡、表现和状态文档已同步；八类设施具备加载校验和逐类非法配置负例，综合契约测试 `16/16` 通过。`Suppressed`、`Silent`、不可摧毁、雷达接触、动态水雷和 `Docked/Servicing` 已有区分表现。等 Cost 港湾批次复用同日 2.6 的 `1 -> 3 -> 20` 与侧别交换正式证据，没有重复执行 20/40 局回归。

- 2026-07-04：港湾均衡验证编队已统一为双方 `12 Cost`，并由关卡加载校验拒绝预算不一致。双方标准运行时 AI 依次通过 `1 -> 3 -> 20` 固定种子与侧别交换门禁；正式批次 `40/40` 完成，原玩家阵容胜率 `7.5%`、玩家出生侧胜率 `47.5%`、设施使用局占比 `100%`、超时率 `2.5%`、行为异常 `352.48/局`，平均时长 `169.03s`。报告位于 `artifacts/simulations/sim.latest_ai.harbor_equal_cost.gate_20/report.md`；高频设施控制拒绝及目标点落陆路线问题仍作为后续项保留，原计划的独立 `docs/94_ai_navigation_facility_followup.md` 未建立，不影响本轮等 Cost 与报告链路验收，但后续 AI 调整需继续处理。

- 2026-07-02：AI 动态验收改为 `1 局冒烟 -> 3 局小样本 -> 20 局正式样本`，任一阶段失败即停止扩量。当时港湾双方分别为玩家 `12 Cost`、敌方 `9 Cost`，因此该批次只保留为历史运行证据，不再用于阵容、AI 或设施均衡结论；当时正式样本 `20/20` 均以旗舰沉没正常结束，平均时长 `75.76s`，玩家阵容胜率 `35%`，规避恢复率 `95.40%`、设施交互完成率 `50%`、平均协同分 `64.83`、过量伤害率 `1.14%`。

- 2026-06-30：完成 48 名角色主动技能运行时修复。施法距离与效果半径已拆分；下一轮攻击、目标/装甲/潜航/已侦查条件、技能攻击波次、侦察区、航空 HP 与防空折损、开火暴露、零伤害触发和潜艇氧气均进入公共战斗管线；敌方 AI 按爆发、防御、防空、反潜、鱼雷预警、侦查和支援用途选择时机，玩家技能仍永久手动。新增 417 项全角色技能检查，连同 285 项第二期配置、54 项 AI 量化、7 项 AI 战场输入、123 项场景表现、22 项伤害统计、22 项模拟器和核心回归通过。最新 AI 的 3v3/5v5 共 20 局全部正常结束，合计开火 8313 发、释放技能 223 次；场景表现仍只打印第二期 20 名角色缺少切入图的既有日志。

- 2026-06-30：AI 控制器完成首批真实战场输入接入。除模式/战术/目标迟滞、局部压力、完整开火纪律和规避恢复外，目标威胁现读取实际武器、射程与正式期望伤害，侧翼读取目标舷角和交叉火力，炮线读取射界、岛岸、友军当前/短期位置，路线读取实际规划长度、威胁、边界和拥堵；鱼雷风险读取真实伤害与转向时间，并比较四类规避航点。54 项 AI 量化、7 项战场输入、1684 项核心及 22 项模拟器检查通过；双方最新运行时 AI 的 3v3/5v5 共 20 个固定种子全部正常结算，平均时长 `73.50s`。该批次仍未提供完整行为指标与侧别交换，胜率只反映当前固定阵容，不作为 AI 完成度或平衡结论。
- 2026-06-30：新增正式 `AIObservation` 决策快照。AI 的目标选择、局部兵力、路线威胁、设施任务、技能侦察、敌舰分类和鱼雷威胁改为读取阵营过滤数据；隐藏敌舰的实时位置、HP 与旗舰状态不能改变决策，未知投射物、设施和水雷不会进入观察。新增 12 项观察隔离检查，并保持 54 项量化与 9 项真实战场输入检查通过。
- 2026-06-30：设施 AI 从固定优先级减距离升级为正式量化任务。占领/激活读取设施价值、生存、路径、舰种适配、时限、争夺压力与分配饱和；己方关键设施受可见敌军逼近时可生成达到阈值的防守任务；维修与任务失效均会重新评分，过期任务会清除。12 项设施任务检查覆盖多舰分流、内守/入口拦截/远程火援、防守阈值和威胁消失后的任务恢复。
- 2026-06-30：AI 主要武器提交时新增短期预计伤害预留，目标选择与开火窗口会扣除其他舰已承诺的预计伤害，预留在预计命中窗结束或目标沉没后自动清除；真实战场输入专项扩展为 9 项。
- 2026-06-30：技能 AI 新增语义效果预留与显式攻击协同。侦察、防空、反潜、鱼雷预警、区域支援和控制技能按用途、阵营、位置与半径避免重复覆盖；进攻爆发技能使用准备度、目标窗口、侧翼、伤害预留和任务时机计算协同分，达到 `70` 才提交。新增 9 项协同检查，并保持 417 项技能运行时检查通过。
- 2026-06-30：敌方舰队新增弹性编组、职责和阵列修正。每组保持 `1-4` 艘，按旗舰/稳定实体顺序选举与接替组长，舰种映射为 `Scout/Screen/Fixer/Flanker/FireSupport`，设施任务临时覆盖为 `ObjectiveRunner`；搜索、接敌、近岸、航母护卫和已知投射物威胁可选择横队、楔形、纵队、护卫圈或疏散阵。新增 12 项编组阵列检查。
- 2026-06-30：批量模拟新增 AI 行为指标和简单/标准/困难 Profile。当时的难度 Profile 曾包含规避路线候选数字段；该字段已于 2026-07-17 退出，当前难度只调整决策周期、技能/协同阈值、目标确认和效果预留，不修改属性、观察权限或共享导航预算。该轮新增 12 项难度检查，模拟器回归增至 26 项。
- 2026-07-01：修复模拟器完整 AI 的阵营不对称：玩家侧现与敌方同样建立/刷新编组并使用本阵营合法设施支援。标准 Profile 的 3v3/5v5 十种子侧别交换共 40/40 局完成，玩家出生侧总胜率 `50%`；3v3 原敌方阵容在两侧均胜，5v5 原玩家阵容在两侧均为 `40%`，说明出生侧偏差已消除但阵容差异仍明显。平均时长 `105.24s`，单位分钟模式/战术/目标切换 `0.94/1.31/3.54`，规避恢复率 `89.30%`、平均协同分 `76.76`、过量伤害率 `0.96%`。新增 12 项路线/掩体/卡住恢复检查；当时港湾仅完成单种子运行，后续结果见 2026-07-02 条目。
- 2026-06-30：所有对海舰炮改为逐发独立椭圆高斯落点，统一 sigma 系数以厌战号 `1080` 射程、`14°` 散布和 `150` 游戏内舰长标定；横向 `1σ=150`、纵向 `1σ=75`，并随实际距离、`spread` 与 `WeaponSpread` 同比缩放。领域伤害、岛岸阻挡、`WeaponFired.impact_positions`、炮弹轨迹和瞄准 `1σ` 椭圆现共享同一落点契约。1673 项核心检查、50 项 AI 量化检查、123 项场景表现检查、285 项第二期配置检查、22 项伤害统计检查和 22 项模拟器检查通过；启动解析正常，仍只打印第二期 20 名角色缺少切入图的既有日志。
- 2026-06-30：局内伤害倍率由 `0.1` 调整为 `0.25`，设计量纲保持不变；基础伤害、作战能力系数和装甲减伤系数同步换算，最终局内伤害为设计结算的 25%。1665 项核心检查、22 项伤害统计检查、285 项第二期配置检查和 22 项模拟器检查通过。
- 2026-06-30：伤害公式新增设计量纲到局内量纲的统一 `0.1` 倍率；基础伤害、作战能力系数和装甲减伤系数同步换算，使局内最终伤害严格为设计结算的 10%。1665 项核心检查、22 项伤害统计检查、285 项第二期配置检查、22 项模拟器检查和 50 项 AI 量化检查通过；五个模式共 30/30 局正常结算，1v1 平均时长上升至约 207 秒。启动解析正常，仍仅打印第二期 20 名角色缺少切入图的既有日志。
- 2026-06-29：模拟器新增 `LatestRuntimeAI` 双方完整 AI 策略，正常玩家控制默认不变。当前战斗切片固定阵容下，3v3 与 5v5 各运行 10 个固定种子，20/20 局正常结束：3v3 玩家阵容 1 胜 9 负，5v5 玩家阵容 10 胜 0 负；22 项模拟器检查、50 项 AI 量化检查和 1643 项核心检查通过。
- 2026-06-29：AI 设计首轮接入 `BattleSession` 与战斗界面。敌方使用量化目标、模式迟滞、局部战术、主要武器窗口、技能收益和即时规避；玩家默认静止、副武器自动、主武器与技能不自动，并可用 `Z/X/C/V`、`Cmd/Alt` 舰队范围及 `G` 镜头跟踪。1643 项核心检查、50 项量化检查（11 组场景）、123 项场景表现检查及 Godot 启动解析通过；场景检查仍仅打印第二期 20 名角色缺图的既有日志。编组协同、预留与完整动态指标仍按“部分完成”管理。
- 2026-06-29：新增逐舰伤害分类与查询接口；直接伤害可按主炮、副炮、鱼雷、航空、防空、反潜、技能等互斥类别及武器 ID 汇总，Buff 增伤按来源舰船与技能单独归因且不重复计入总伤害。22 项专项检查、1630 项核心检查、14 项模拟器检查和 119 项场景表现检查通过。
- 2026-06-29：战斗模拟器独立 12 项检查通过；同一清单和种子可复现，报告文件可落盘，有效伤害不计入过量伤害且无目标落区不会生成空统计单位。随后单独运行 `sim.smoke.single_battle` 的 6 个固定种子，6/6 正常结束并生成 JSONL、JSON、CSV 和 Markdown 报告。
- 2026-06-29：模拟器已支持分别声明玩家侧与敌方侧策略，双方基线 AI 开火检查增至 14 项并通过。四种开阔海域规模各运行 6 个固定种子，共 24/24 局正常结算；报告按场景汇总双方射击、胜率与时长。港湾未纳入本轮对称实验，因为其设施任务仍包含敌方专用兼容 AI。
- 2026-06-29：模拟器已支持阵容与出生侧配对交换，并直接使用 `damage_statistics.gd` 输出每局每舰的有效伤害、承伤、过量伤害、武器分类和 Buff 贡献。四种开阔海域规模 × 6 种子 × 原始/交换共 48/48 局正常结束：玩家/敌方出生侧各胜 24 局，但原敌方阵容跨两侧合计 48:0，确认主要差距来自阵容而非出生侧。
- 2026-06-29：鱼雷距离观测、阵营共享、角色技能距离修正、逐座管组冷却/射界、`1s` 发射间隔、最大射程 `80` 理想雷道间距，以及“基础 sigma 为相邻张角 `20%`、海况/风速放大至最多 `3x`”的逐雷高斯角误差均已接入；1630 项核心检查、35 项 AI 量化检查、119 项场景表现检查和 285 项第二期配置检查通过。启动检查正常退出，但仍打印第二期 20 名角色缺少切入图的既有错误；地形生产门禁、9 个非法数据负例和 10 项制作插件检查沿用 2026-06-28 的通过结果。
- 局部飑线已把既有风暴暗云、斜向雨线和雨点涟漪 AI 母版合成为 512 x 512 正式透明天气层，来源链记录在天气区域 manifest，并纳入确定性生产门禁。
- 五个关卡各执行 6 局批量模拟，合计 30 局均正常结束；港湾入口 6/6 完成结算，岸炮伤害已进入战斗统计。
- 港湾入口已人工检查 1920 x 1080、3840 x 2160、F9 全知调试、海雾局部和飑线局部截图。正式总览为 `assets/environment/qa/scene_combat_contact_sheet.png`。
- 设施、任务、水雷和九类环境标识及其战斗语义已统一整理在 `docs/18_facility_weather_effect_design.md`；小地图会在局部环境中心显示对应标识。
- 场景测试仍会打印第二期 20 名角色缺少切入图的既有错误；测试会排除这些候选并继续通过，此缺口与地形工单无关。
